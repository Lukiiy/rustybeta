[package]
name = "server"
version = "0.1.0"
edition = "2024"

[lib]
name = "server"
path = "src/lib.rs"

[dependencies]
protocol = { path = "../protocol" }
world = { path = "../world" }
entity = { path = "../entity" }